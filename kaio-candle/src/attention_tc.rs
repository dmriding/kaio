//! `AttentionTcOp` (CustomOp3, `causal: bool` on the struct) + `attention_tc()` +
//! `attention_tc_causal()` wrappers — D5 deliverable.
//!
//! Stub at D1; content lands alongside the bit-exact integration test quad.
